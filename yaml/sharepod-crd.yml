---
apiVersion: apiextensions.k8s.io/v1beta
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.4.0
  creationTimestamp: null
  name: sharepods.kubeshare.nthu
spec:
  group: kubeshare.nthu
  names:
    kind: SharePod
    listKind: SharePodList
    plural: sharepods
    singular: sharepod
    shortNames:
    - shp
  scope: Namespaced
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        description: Sharepod describes an Faas-Share instance
        type: object
        required:
        - spec
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            description: 'Object meata for stroring annotations for sharepod and labels for openfaaas'
            type: object
            required:
            - labels
            - annotations
            properties:
              name:
                type: string
              annotations:
                type: object
                additionalProperties:
                  type: string
              labels:
                type: object
          spec:
            description: 'Sharepod Spec is the spec for a sharepod resoruce'
            type: object
            required:
            - containers
            properties:
              containers:
                type: object
                
    served: true
    storage: true
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: []
  storedVersions: []
